$if mode=emacs
  # This is missing on OSX for some reason...
  Control-a: beginning-of-line

  # Allow searching from command line (with arrow keys)
  "\e[A": history-search-backward
  "\e[B": history-search-forward
$endif
