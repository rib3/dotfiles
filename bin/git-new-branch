#!/bin/sh

die() {
  [ -n "$1" ] && echo "${1}"
  exit 1
}

if [ -z "$1" ]; then
  die "missing branch name"
fi

BASE="${2:-origin/master}"

git fetch || die

git checkout --no-track -b "${1}" "${BASE}"
